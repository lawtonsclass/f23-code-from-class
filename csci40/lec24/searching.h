#ifndef SEARCHING_H
#define SEARCHING_H

#include <vector>

int linearSearch(const std::vector<int>& v, int valToSearchFor);
int binarySearch(const std::vector<int>& v, int valToSearchFor);

#endif /* end of include guard: SEARCHING_H */
